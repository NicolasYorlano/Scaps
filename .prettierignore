dist
coverage
package-lock.json

# Prettier no toca Markdown: son documentos que se escriben y revisan a mano.
# Al formatearlos rellena las tablas con espacios, lo que genera diffs enormes
# y puramente cosméticos, y deja el archivo más incómodo de editar.
*.md
